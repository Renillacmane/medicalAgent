# Implementation Plan

> Auto-generated by planning agent. Source of truth for requirements: `specs/`.
> Cycle: 48 — gap analysis refreshed against codebase on 2026-03-03.
>
> **Backend verification** (unchanged from Cycle 47): `PatientsService` has 8 methods (getProfile, updateProfile, getExams, getDocuments, getVitals, getLatestVitalsByPeriod, createVital, getPatientSnapshotForAgent). Constructor injects `User`, `UserVital`, `UserExam`, `UserDocument` — no `@InjectModel(UserMedication.name)`. `PatientsController` has 8 routes: profile (GET/PATCH), exams (GET), documents (GET + POST upload), vitals (GET + GET latest-by-period + POST) — no medication or settings endpoints. DTOs: `create-vital.dto`, `update-profile.dto`, `patient-snapshot.dto` — no `CreateMedicationDto` / `UpdateMedicationDto` / `UpdateSettingsDto`. `backend/src/notifications/` does not exist. `AppModule` imports: ConfigModule, DatabaseModule, AuthModule, PatientsModule, HealthModule, RecommendationsModule — no NotificationsModule. `UserMedication` model registered in `PatientsModule` `MongooseModule.forFeature` but **not injected** in `PatientsService` constructor. `User.notificationSettings` present with correct defaults (`{ dailyRecommendations: false, vitalsReminder: true, medicationReminder: false }`). No device token / push subscription fields on User schema. `getPatientSnapshotForAgent` has commented-out `getMedications` call (Phase 2+ placeholder); snapshot returns `profile` and `vitals` only. `backend/src/common/utils/` has `date.utils.ts` (calculateAge) and `health-boundaries.ts` (BMI/BP/HR/SpO₂ categories). Schema changes for `UserMedication` (timesPerDay, reminderTimes, sourceDocumentId) and `User` (notificationSettings) are in working tree (uncommitted).
>
> **Frontend verification** (Cycle 48): `patients.service.ts` has 13 functions (getProfile, updateProfile, getVitals, getLatestVitalsByPeriod, createVital, getExams, getDocuments, uploadDocument, getMedications, createMedication, updateMedication, deleteMedication, batchCreateMedications). `api.ts` exports `authGet`, `authPost`, `authPatch`, `authDelete`, `authPostFormData`. `types/health.ts` has `Medication`, `CreateMedicationPayload`, `UpdateMedicationPayload`, `BatchMedicationItem`, `BatchCreateMedicationsPayload`, `BatchCreateMedicationsResponse` types. `PatientProfile` type in `types/profile.ts` has **no `notificationSettings` field**. `PrescriptionExtractedData` type in `types/health.ts` has `medications` array with `name`, `dosage`, `frequency`, `duration?`. No `MedicationReviewDialog` component. No frequency-to-schedule inference in `lib/`. `Dashboard.tsx` shows vitals table only — no medications section. `Profile.tsx` has Account + Vitals/preferences sections — no notification settings. `sw.js` handles install/activate/fetch/sync only — no push/notificationclick handlers. Capacitor deps: `@capacitor/core`, `@capacitor/cli`, `@capacitor/android`, `@capacitor/ios` only — no push-notifications/local-notifications plugins. Recommendations page has no notification bubble. `AddPrescriptionForm` shows inline success message + resets after 2s — no medication dialog; upload response (`{ id, status, message }`) does not include extracted data. No `GET /patients/documents/:id` single-document endpoint exists; list endpoint can filter by returned `id`. `My Health` page displays prescription medication names from `extractedData` in a read-only table; this is document display, not the medication CRUD system.
>
> **Upload flow detail** (relevant to RX-DIALOG-UI-8): `DocumentProcessorService.processDocument()` is a shared pipeline returning `{ id, status }` only. `processPrescription()` calls it and propagates the same shape. The `extractedData` (including `medications` array) is already persisted in the `userdocuments` collection by the time the controller returns. The cleanest approach is to modify `processDocument` (or just the prescription path) to also return `extractedData`, then surface it in the controller response — avoids an extra fetch.

---

## Previously completed specs (pruned)

All tasks done for: **Add Vitals & Lab Results — API** (API-1…API-9), **Add Vitals & Lab Results — UI** (UI-ADD-1…UI-ADD-6), **Daily Recommendations — API** (REC-API-1…REC-API-6), **Daily Recommendations — UI** (REC-UI-1…REC-UI-5), **My Health — UI** (HEALTH-UI-1…HEALTH-UI-7).

---

## Spec: Medication Reminders (`specs/medication-reminders.md`)

### Schema status

- [x] **MED-SCHEMA-1** `UserMedication` schema has all required fields: `name`, `dosage`, `frequency`, `timesPerDay`, `reminderTimes`, `startDate`, `endDate`, `isActive`, `sourceDocumentId` (plus `activeSubstance`, `purpose`)
  - Verified in `backend/src/patients/schemas/user-medication.schema.ts`; collection `usermedications`; indexed on `userId`.
  - Model registered via `MongooseModule.forFeature` in `PatientsModule` but **not injected** in `PatientsService` — MED-API-1 must add `@InjectModel(UserMedication.name)`.
  - Schema changes are uncommitted in working tree. Should be committed as part of MED-API-1.

### Backend CRUD

- [x] **MED-API-1** `GET /patients/medications` — return authenticated user's medications (sorted by name)
  - Injected `UserMedication` model in `PatientsService`; added `getMedications(userId)` method; added `@Get('medications')` to `PatientsController`.
  - Uncommented `getMedications` call in `getPatientSnapshotForAgent`; added `PatientMedication` interface to `PatientSnapshotDto` (optional field); updated `patient-snapshot.formatter.ts` to include medication context in LLM prompts.

- [x] **MED-API-2** `POST /patients/medications` — create a single medication for the authenticated user
  - Spec AC: "When a medication is created (manually or from prescription), it includes timesPerDay and reminderTimes fields."
  - Created `CreateMedicationDto` with validation (name required, reminderTimes HH:mm format via regex, timesPerDay 1-10, optional sourceDocumentId as MongoId); added `createMedication()` to `PatientsService` and `@Post('medications')` to `PatientsController`.

- [x] **MED-API-3** `PATCH /patients/medications/:id` — update a medication (including reminder times, active status)
  - Spec AC: "User can tap a medication to edit its reminder settings."
  - Created `UpdateMedicationDto` (all fields optional, same validation as create); added `updateMedication()` to `PatientsService` with ownership check (userId match); added `@Patch('medications/:id')` to `PatientsController` returning 404 if not found.

- [x] **MED-API-4** `DELETE /patients/medications/:id` — hard-delete a medication
  - Spec AC: "DELETE /patients/medications/:id — remove medication."
  - Added `deleteMedication()` to `PatientsService` with ownership check (userId match via `deleteOne`); added `@Delete('medications/:id')` to `PatientsController` returning 404 if not found, `{ success: true }` on success.

### Frontend — Dashboard medications table

- [x] **MED-UI-1** Add `getMedications()` and `createMedication()` / `updateMedication()` / `deleteMedication()` to `frontend/src/services/patients.service.ts`
  - Added `authDelete` helper to `frontend/src/lib/api.ts`.
  - Added `Medication`, `CreateMedicationPayload`, `UpdateMedicationPayload`, `BatchMedicationItem`, `BatchCreateMedicationsPayload`, `BatchCreateMedicationsResponse` types to `frontend/src/types/health.ts`.
  - Added `getMedications`, `createMedication`, `updateMedication`, `deleteMedication`, `batchCreateMedications` to `patients.service.ts`.

- [x] **MED-UI-2** Add "Current Medications" section to `Dashboard.tsx` — table with name, dosage, frequency, status
  - Spec AC: "When the user views the dashboard, a 'Current Medications' section displays active medications. Each row shows: medication name, dosage, frequency, and status (active/inactive)."
  - Added "Current Medications" section with table (name, dosage, frequency, status); fetches via `getMedications()`; loading/empty/error states consistent with vitals section.

- [x] **MED-UI-3** Medication row tap opens edit view for reminder settings (inline or modal)
  - Spec AC: "User can tap a medication to edit its reminder settings."
  - Implemented: tap/click on medication row opens `MedicationEditModal` with editable reminder times, timesPerDay, start/end date, isActive; Save calls PATCH and refetches list; Cancel closes without saving.

### Frontend — Local notification scheduling

- [x] **MED-UI-4** Schedule local notifications for each active medication based on `reminderTimes` (when medication reminders are enabled in settings)
  - Spec AC: "When medication reminders are enabled in settings and a medication has reminderTimes configured, local notifications are scheduled."
  - Also: "When endDate is reached, reminders for that medication stop." — scheduling logic must respect `isActive`, `startDate`/`endDate`, and `notificationSettings.medicationReminder`.
  - Implemented: `scheduleMedicationReminders(medications, settings)` in `local-notifications.ts` filters by isActive, non-empty reminderTimes, and date within startDate/endDate; cancels existing medication IDs (≥1000) then schedules daily notifications with body "Time to take [name] ([dosage])". Dashboard fetches medications + settings and calls it when medicationReminder is enabled.

- [x] **MED-UI-5** When user updates `reminderTimes`, cancel existing notifications and reschedule
  - Spec AC: "When the user updates reminderTimes, existing notifications are cancelled and new ones are scheduled."
  - Implemented: after saving changes in `MedicationEditModal`, Dashboard refetches medications and settings, then calls `scheduleMedicationReminders` so existing medication notifications are cancelled and new ones are scheduled based on updated reminderTimes.

- [x] **MED-UI-6** When user disables medication reminders in settings, cancel all medication notifications
  - Spec AC: "When the user disables medication reminders in settings, all medication notifications are cancelled."

---

## Spec: Prescription Medication Dialog (`specs/prescription-medication-dialog.md`)

### Backend — batch endpoint

- [x] **RX-DIALOG-API-1** `POST /patients/medications/batch` — create/update multiple medications in one call, linked to a source document
  - Spec: Request body `{ sourceDocumentId, medications: [...] }`; response `{ created, updated, medications }`.
  - Upsert logic: if medication with same name exists for user, update it; otherwise create.
  - Created `BatchCreateMedicationsDto` (with nested `BatchMedicationItemDto` validated via `ValidateNested`); added `batchCreateMedications()` to `PatientsService` with case-insensitive name matching for upsert; added `@Post('medications/batch')` to `PatientsController`. Also handles duplicate names within the same batch (second occurrence updates the first).

### Frontend — dialog component

- [x] **RX-DIALOG-UI-1** Create `MedicationReviewDialog` component — modal that displays extracted medications as editable cards
  - Spec AC: "When a prescription is uploaded and processed successfully, the medication dialog opens automatically. The dialog displays all medications extracted from the PDF as editable cards."
  - Header: "Review Medications" with prescription filename.
  - Implemented `MedicationReviewDialog` shell component in `frontend/src/components/medications/MedicationReviewDialog.tsx`; wiring from `AddPrescriptionForm` will be handled in RX-DIALOG-UI-8.

- [x] **RX-DIALOG-UI-2** Implement frequency-to-schedule inference logic (frontend utility)
  - Spec: "once daily" → 1 / ["08:00"]; "twice daily"/"BID" → 2 / ["08:00","20:00"]; "TID" → 3; "QID" → 4; default → 1.
  - Implemented `inferTimesPerDay`, `suggestReminderTimes`, and `inferMedicationSchedule` in `frontend/src/lib/medication-schedule.ts`, including basic duration parsing for day/week-based strings to infer `endDate`.

- [x] **RX-DIALOG-UI-3** Medication card fields: name, dosage, frequency, timesPerDay, reminderTimes, startDate, endDate — all editable
  - Implemented editable fields for name, dosage, frequency, timesPerDay, reminderTimes, startDate, and endDate on each medication card in `MedicationReviewDialog.tsx`.
  - Reminder times are displayed as a comma-separated HH:mm list and kept in component state as a string array.
  - When the user changes timesPerDay, reminderTimes are re-suggested using `suggestReminderTimes` (user can still manually override the reminder times text field).

- [x] **RX-DIALOG-UI-4** "+" button to manually add a medication card with defaults
  - Spec AC: "When the user clicks '+', a new empty medication card is added to the list."
  - Default values: empty name, "once daily", today as start date.

- [x] **RX-DIALOG-UI-5** Scrollable medication list; dialog does not exceed viewport height
  - Spec AC: "When there are more than 3-4 medications, the list is scrollable within the dialog."
  - Dialog uses `max-h-[calc(100vh-2rem)]` and flex column; list area is `flex-1 min-h-0 overflow-y-auto` so it scrolls within the dialog without exceeding viewport.

- [x] **RX-DIALOG-UI-6** Save action — call batch endpoint, show success, close dialog
  - Spec AC: "When the user clicks 'Save', all medications are persisted to the backend. After save, the dialog closes and the user sees a success message."
  - Validation: name required; startDate required; reminderTimes valid HH:mm.
  - Implemented: `handleSave` validates (name, startDate, HH:mm); calls `batchCreateMedications`; on success invokes `onSaveSuccess` then `onClose`. Dialog accepts `sourceDocumentId` and `onSaveSuccess` props. Per-card validation errors and save-error message displayed.

- [x] **RX-DIALOG-UI-7** Cancel action — close dialog without saving; uploaded document remains
  - Spec AC: "When the user clicks 'Cancel', the dialog closes without saving."
  - Implemented: Cancel button calls `onClose`; backdrop click and Escape key also close without saving; no medications created/updated; document remains.

- [x] **RX-DIALOG-UI-8** Wire `AddPrescriptionForm` to open `MedicationReviewDialog` on successful upload
  - Backend: `DocumentProcessorService.processDocument()` now returns `extractedData` on success; `processPrescription` return type includes it. `PatientsController.uploadDocument` includes `extractedData` in the response for prescription uploads.
  - Frontend: `uploadDocument` return type includes optional `extractedData`. On successful prescription upload with `extractedData`, `AddPrescriptionForm` opens `MedicationReviewDialog` with document id/name and initial medications built via `inferMedicationSchedule`. On dialog save success, shows success message and calls `onSuccess`. If no `extractedData`, keeps previous behavior (success message + reset after 2s).

---

## Spec: Notifications (`specs/notifications.md`)

> Implementation follows phased approach per `docs/notifications-research.md`:
> **Phase 1** — Settings API + Capacitor plugins + local notifications (no FCM/APNs setup needed).
> **Phase 2** — Device registration backend + Capacitor push notifications (requires FCM/APNs).
> **Phase 3** — PWA web push (optional; note iOS 16.4+ only, EU users excluded on iOS 17.4+).

### Phase 1: Settings API + local notifications

- [x] **NOTIF-API-3** `GET /patients/settings` — return notification preferences from user document
  - `notificationSettings` field already exists on User schema with correct shape and defaults (`{ dailyRecommendations: false, vitalsReminder: true, medicationReminder: false }`).
  - Added `GET /patients/settings` in `PatientsController`; added `getSettings(userId)` in `PatientsService` returning `{ notificationSettings }` with schema defaults applied.
  - Note: spec says `GET /users/settings` but existing API convention is `/patients/`; use `/patients/settings`.

- [x] **NOTIF-API-4** `PATCH /patients/settings` — update notification preferences
  - Spec AC: "When the user toggles a notification type, the setting is persisted to the backend."
  - Added `UpdateSettingsDto` (nested `notificationSettings` with optional `dailyRecommendations`, `vitalsReminder`, `medicationReminder`); added `updateSettings()` to `PatientsService` (merge with current + defaults); added `@Patch('settings')` to `PatientsController`.

- [x] **NOTIF-FE-1** Install `@capacitor/push-notifications` and `@capacitor/local-notifications`; run `npx cap sync`
  - Spec: "Install @capacitor/push-notifications and @capacitor/local-notifications."
  - Added both plugins to `frontend/package.json` (^7.0.1); ran `npm install` and `npx cap sync`; Android and iOS native projects updated.

- [x] **NOTIF-UI-1** Add notification settings section to Profile/Settings page with toggles for each type
  - Spec AC: "When the user opens Settings, they see toggles for: Daily recommendations (off by default), Add vitals reminder (on by default), Medication reminder (off by default)."
  - Added `NotificationSettings` type and `notificationSettings` to `PatientProfile` in `frontend/src/types/profile.ts`. Added `getSettings()` and `updateSettings()` to `patients.service.ts`. Profile page loads settings via `getSettings()`; added "Notifications" section with three toggles (Daily recommendations, Add vitals reminder, Medication reminder). Toggle changes call `PATCH /patients/settings` and update local state.

- [x] **NOTIF-UI-2** Toggle changes call `PATCH /patients/settings` and update local state; reschedule local notifications accordingly
  - Spec AC: "When the user toggles a notification type, the setting is persisted to the backend and local notifications are rescheduled accordingly."
  - After PATCH success, Profile calls `rescheduleFromSettings()` from `frontend/src/lib/local-notifications.ts`. On native only: daily at 8:00, vitals at 9:00; when medication reminder off, cancels all medication notification IDs (≥1000).

- [x] **NOTIF-FE-8** Schedule daily local notification for "daily recommendations" when enabled (8:00 AM default)
  - Spec AC: "When enabled and the scheduled time arrives, a local notification fires: 'Your daily recommendations are ready'."

- [x] **NOTIF-FE-9** Schedule daily local notification for "add vitals reminder" when enabled (9:00 AM default)
  - Implemented via `rescheduleFromSettings` in `frontend/src/lib/local-notifications.ts`: when `notificationSettings.vitalsReminder` is enabled, schedules a daily Capacitor local notification at 9:00 with body "Time to log your vitals"; when disabled, the notification is cancelled.

### Phase 2: Device registration + push notifications (Capacitor native)

- [x] **NOTIF-API-1** `POST /notifications/register` — authenticated user submits device token or web push subscription; store in user document or separate collection
  - `NotificationsModule`, `NotificationsController`, and `NotificationsService` exist under `backend/src/notifications/`.
  - `AppModule` imports `NotificationsModule`.
  - `User` schema extended with `notificationDevices` array to store device tokens and web push subscriptions per user.

- [x] **NOTIF-API-2** `DELETE /notifications/register` — remove device token/subscription for the authenticated user
  - Spec AC: "When the user logs out, the device token is unregistered from the backend."
  - Implemented `DELETE /notifications/register` in `NotificationsController` using `RegisterNotificationDto` as the request body.
  - Added `unregister()` method to `NotificationsService` that removes a matching device by `deviceToken` or web push `endpoint` from `notificationDevices` for the authenticated user and returns `{ success: boolean }`.
  - Backend validation: backend `npm run lint`, `npm run test`, and `npm run build` all pass.

- [x] **NOTIF-FE-2** On app launch (authenticated + native), request notification permission and register device token with backend
  - Spec AC: "When the user opens the app on iOS/Android and is authenticated, the app requests notification permission; if granted, it registers the device token with the backend."
  - No push notification initialization code exists. Add to app layout or a dedicated hook.

- [x] **NOTIF-FE-3** Handle denied permission gracefully — show info in settings that notifications are disabled at OS level
  - Implemented OS-level permission check in `Profile` notifications section using Capacitor Push Notifications.
  - When permission is denied on a native device, the settings page shows an inline message explaining that notifications are disabled at the OS level and how to re-enable them.

- [ ] **NOTIF-FE-4** On logout, unregister device token from backend
  - Spec AC: "When the user logs out, the device token is unregistered from the backend."
  - `authDelete` helper in `api.ts` already exists (added in MED-UI-1).

- [ ] **NOTIF-FE-5** Handle incoming push notifications — display and deep-link on tap
  - Spec AC: "When the user taps the notification, the app opens to /recommendations (or /add or /dashboard depending on type)."

- [ ] **NOTIF-UI-3** When notification permission is denied at OS level, disable toggles with explanation text
  - Spec AC: "When notification permission is denied at OS level, toggles are disabled with explanation text."

### Phase 3: PWA web push (browser)

- [ ] **NOTIF-FE-6** Add push event handler to `sw.js`; handle `notificationclick` for deep-linking
  - Spec: "Service worker handles push event and displays notification. On notification click, open appropriate page."
  - `sw.js` exists with install/activate/fetch/sync handlers but no push or notificationclick handling.

- [ ] **NOTIF-FE-7** On opt-in (web), subscribe via `PushManager.subscribe()` with VAPID keys and send subscription to backend
  - Spec: "On opt-in, send subscription object to backend via /notifications/register."
  - No VAPID key generation or push subscription code exists.

### Recommendations page bubble

- [ ] **NOTIF-UI-4** Show dismissable info bubble on `/recommendations` when daily recommendations notifications are not enabled
  - Spec AC: "When the user visits /recommendations and has not enabled daily recommendations notifications, a dismissable info bubble appears."
  - Bubble text: "Enable notifications to get daily reminders" with link to Settings.
  - Dismissable; once dismissed, don't show again for the session.
  - Recommendations page (`frontend/src/app/(app)/recommendations/page.tsx`) has no notification bubble.

---

## Priority order

| # | Task ID | Summary | Depends on | Spec |
|---|---------|---------|------------|------|
| 1 | MED-API-1 | `GET /patients/medications` endpoint (inject model + service method + controller) | — | medication-reminders |
| 2 | MED-API-2 | `POST /patients/medications` endpoint + DTO | MED-API-1 (shared model injection) | medication-reminders |
| 3 | MED-API-3 | `PATCH /patients/medications/:id` endpoint | MED-API-1 | medication-reminders |
| 4 | MED-API-4 | `DELETE /patients/medications/:id` endpoint | MED-API-1 | medication-reminders |
| 5 | RX-DIALOG-API-1 | `POST /patients/medications/batch` endpoint | MED-API-2 | prescription-medication-dialog |
| 6 | MED-UI-1 | Frontend medication service functions + `authDelete` helper | MED-API-1 | medication-reminders |
| 7 | RX-DIALOG-UI-2 | Frequency-to-schedule inference utility | — | prescription-medication-dialog |
| 8 | RX-DIALOG-UI-1 | `MedicationReviewDialog` component (shell) | — | prescription-medication-dialog |
| 9 | RX-DIALOG-UI-3 | Editable medication card fields | RX-DIALOG-UI-1, RX-DIALOG-UI-2 | prescription-medication-dialog |
| 10 | RX-DIALOG-UI-4 | "+" add medication button | RX-DIALOG-UI-1 | prescription-medication-dialog |
| 11 | RX-DIALOG-UI-5 | Scrollable list + viewport constraint | RX-DIALOG-UI-1 | prescription-medication-dialog |
| 12 | RX-DIALOG-UI-6 | Save action (batch API + validation) | RX-DIALOG-API-1, RX-DIALOG-UI-3 | prescription-medication-dialog |
| 13 | RX-DIALOG-UI-7 | Cancel action | RX-DIALOG-UI-1 | prescription-medication-dialog |
| 14 | RX-DIALOG-UI-8 | Wire AddPrescriptionForm → dialog on upload success | RX-DIALOG-UI-6 | prescription-medication-dialog |
| 15 | MED-UI-2 | Dashboard "Current Medications" table | MED-UI-1 | medication-reminders |
| 16 | MED-UI-3 | Medication row tap → edit reminder settings | MED-UI-2, MED-API-3 | medication-reminders |
| 17 | NOTIF-API-3 | `GET /patients/settings` endpoint | — | notifications |
| 18 | NOTIF-API-4 | `PATCH /patients/settings` endpoint | NOTIF-API-3 | notifications |
| 19 | NOTIF-FE-1 | Install Capacitor notification plugins | — | notifications |
| 20 | NOTIF-UI-1 | Notification settings toggles on Profile page + type updates | NOTIF-API-3, NOTIF-API-4 | notifications |
| 21 | NOTIF-UI-2 | Toggle persists + reschedules local notifications | NOTIF-UI-1, NOTIF-FE-1 | notifications |
| 22 | NOTIF-FE-8 | Schedule daily recommendations local notification | NOTIF-FE-1, NOTIF-UI-1 | notifications |
| 23 | NOTIF-FE-9 | Schedule add-vitals-reminder local notification | NOTIF-FE-1, NOTIF-UI-1 | notifications |
| 24 | MED-UI-4 | Schedule medication local notifications | NOTIF-FE-1, MED-UI-1 | medication-reminders |
| 25 | MED-UI-5 | Reschedule on reminderTimes update | MED-UI-4 | medication-reminders |
| 26 | MED-UI-6 | Cancel all medication notifications when disabled | MED-UI-4, NOTIF-UI-1 | medication-reminders |
| 27 | NOTIF-UI-4 | Recommendations page notification bubble | NOTIF-API-3 | notifications |
| 28 | NOTIF-API-1 | `POST /notifications/register` endpoint + module | — | notifications |
| 29 | NOTIF-API-2 | `DELETE /notifications/register` endpoint | NOTIF-API-1 | notifications |
| 30 | NOTIF-FE-2 | Request permission + register device token | NOTIF-API-1, NOTIF-FE-1 | notifications |
| 31 | NOTIF-FE-3 | Handle denied permission gracefully | NOTIF-FE-2 | notifications |
| 32 | NOTIF-FE-4 | Unregister token on logout | NOTIF-API-2, NOTIF-FE-2 | notifications |
| 33 | NOTIF-FE-5 | Handle incoming push + deep-link on tap | NOTIF-FE-2 | notifications |
| 34 | NOTIF-UI-3 | Disabled toggles when OS permission denied | NOTIF-UI-1, NOTIF-FE-3 | notifications |
| 35 | NOTIF-FE-6 | PWA: push event handler in sw.js | — | notifications |
| 36 | NOTIF-FE-7 | PWA: PushManager.subscribe() + VAPID | NOTIF-API-1 | notifications |
