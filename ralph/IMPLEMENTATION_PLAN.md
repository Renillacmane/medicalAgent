# Implementation Plan

> Auto-generated by planning agent. Source of truth for requirements: `specs/`.
> Cycle: 55 — gap analysis refreshed against codebase on 2026-03-07.
>
> **Backend verification**: `PatientsService` has medication CRUD, settings, getDocuments (list), getDocument (single by id). `PatientsController` has profile, settings, exams, documents (GET list, GET :id, POST upload), medications (GET, POST, PATCH, DELETE, POST batch), vitals. `NotificationsModule` exists; User has `notificationDevices` and `notificationSettings`; recommendations DTO has `littleThingRight`. Upload response includes `extractedData` for prescription. `documentType: lab_result` supported in upload pipeline; `processLabResults` dispatched from upload handler.
>
> **Frontend verification**: `patients.service.ts` has getMedications, createMedication, updateMedication, deleteMedication, batchCreateMedications, getSettings, updateSettings, getDocument(id). `MedicationReviewDialog` and `MedicationEditModal` exist. My Health page: tabs (Vitals, Prescription, Exams), documents table with PDF preview+download, medications table on Prescription tab with origin label. `MedicationEditModal` shows "Original prescription text" section when `sourceDocumentId` is set. `MedicationCalendar` on My Health with dots, month navigation, details modal; inline note when medication reminders disabled. Recommendations page shows "The Little Thing Right" from API and dismissable notification bubble. Push/local notification init in `lib/push-notifications.ts` and `lib/local-notifications.ts`; Profile toggles for notification types; `VitalsCatchUpBubble` on web. Offline vitals: IndexedDB (`healthia-offline`, `pendingVitals`), sync-manager, AddVitalsForm queues on offline/API failure, use-online-status with window online/offline and `@capacitor/network` on native; PendingSyncBadge on Add nav tab, OfflineBanner in AppShell. SW not registered in native (`register-sw.ts`). AppShell uses `App.addListener('appStateChange')` to drain pending vitals when app returns to foreground on native.

---

## Remaining work (priority order)

| # | Task ID | Summary | Depends on | Spec |
|---|---------|---------|------------|------|

*(None — all specs satisfied.)*

---

## Spec: Offline vitals submission — mobile (`specs/offline-vitals-submission-mobile.md`)

### Queue and submit

- [x] When user submits vitals offline (or failed POST), queue in IndexedDB and show "saved offline"
- [x] When online, POST to API; on success normal; on network/server error, fall back to queue + "saved offline"
- [x] Queued vitals use same payload structure as add-vitals-lab-results-api

### Sync when online

- [x] When connectivity returns and app in foreground, queued vitals sync (window `online` + AddVitalsForm mount; on native, `@capacitor/network` status change)
- [x] Successful syncs remove from queue; failed (5xx, timeout) leave for later attempt
- [x] 401 stops sync; queued vitals remain for next authenticated session

### App lifecycle (mobile-specific)

- [x] **OFFLINE-MOBILE-1** When app is brought to foreground (user returns after connectivity was restored while backgrounded), attempt to drain pending vitals queue — `App.addListener('appStateChange')` in AppShell; when `isActive` and native, call `syncPendingVitals()`.

### UI feedback

- [x] Add tab shows badge when queued vitals exist (`PendingSyncBadge` on Add nav tab)
- [x] Offline banner when device offline (`OfflineBanner` in AppShell)
- [x] After offline save: "saved offline", can continue or navigate away; no blocking

### Storage

- [x] Pending vitals in IndexedDB (durable); survives app restart
- [x] Same store for PWA and Capacitor (healthia-offline)

### Optional

- [x] **OFFLINE-MOBILE-2** Use `@capacitor/network` to detect connectivity on mobile; sync when status changes to connected — implemented in `use-online-status.ts` (Network.getStatus, Network.addListener('networkStatusChange')).

---

## Previously completed specs (pruned)

All tasks done for: **Add Vitals & Lab Results — API**, **Add Vitals & Lab Results — UI**, **Daily Recommendations — API**, **Daily Recommendations — UI**, **My Health — UI**, **Medication Reminders**, **Prescription Medication Dialog**, **Notifications**, **My Health – Medication calendar UI**.

---

## Spec references (task mapping)

| Spec | Key tasks |
|------|-----------|
| `offline-vitals-submission-mobile.md` | OFFLINE-MOBILE-1 (foreground drain), OFFLINE-MOBILE-2 (optional @capacitor/network) |
| `medication-reminders.md` | (done) |
| `prescription-medication-dialog.md` | (done) |
| `notifications.md` | (done) |
| `my-health-ui.md` | (done) |
| `my-health-medication-calendar-ui.md` | (done) |
| `add-vitals-lab-results-api.md` | (done) |
| `add-vitals-lab-results-ui.md` | (done) |
| `daily-recommendations-api.md` | (done) |
| `daily-recommendations-ui.md` | (done) |
