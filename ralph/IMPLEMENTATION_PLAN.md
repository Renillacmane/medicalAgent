# Implementation Plan

> Auto-generated by planning agent. Source of truth for requirements: `specs/`.
> Cycle: 51 — gap analysis refreshed against codebase on 2026-03-05.
>
> **Backend verification**: `PatientsService` has medication CRUD, settings, getDocuments (list), getDocument (single by id). `PatientsController` has profile, settings, exams, documents (GET list, GET :id, POST upload), medications (GET, POST, PATCH, DELETE, POST batch), vitals. `NotificationsModule` exists; User has `notificationDevices`; recommendations DTO has `littleThingRight`. Upload response includes `extractedData` for prescription.
>
> **Frontend verification**: `patients.service.ts` has getMedications, createMedication, updateMedication, deleteMedication, batchCreateMedications, getSettings, updateSettings; no `getDocument(id)`. `MedicationReviewDialog` and `MedicationEditModal` exist. My Health page: tabs (Vitals, Prescription, Exams), documents table with PDF preview+download; **does not** call getMedications or show a medications table on Prescription tab. `MedicationEditModal` **does not** show "Original prescription text" section when `sourceDocumentId` is set. No `MedicationCalendar` component. Recommendations page shows "The Little Thing Right" and notification bubble. Push notification init in `lib/push-notifications.ts` (request permission, register, registerDeviceToken). No vitals catch-up bubble on web (PWA).

---

## Remaining work (priority order)

| # | Task ID | Summary | Depends on | Spec |
|---|---------|---------|------------|------|
| 1 | NOTIF-VITALS-CATCHUP | Vitals catch-up bubble on web when reminder enabled but vitals not logged today | — | notifications |
| 2 | CAL-1 | Reusable MedicationCalendar component (month grid, dots, today, month nav) | — | my-health-medication-calendar-ui |
| 3 | CAL-2 | Place calendar on My Health below UserPanel, above tabs | CAL-1 | my-health-medication-calendar-ui |
| 4 | CAL-3 | Map medications + reminderTimes to calendar dots | CAL-1, getMedications/settings | my-health-medication-calendar-ui |
| 5 | CAL-4 | Dot click → details view (date/time + medications at slot) | CAL-1 | my-health-medication-calendar-ui |
| 6 | CAL-5 | Month navigation + "Today" control | CAL-1 | my-health-medication-calendar-ui |
| 7 | CAL-6 | Inline note when medication reminders disabled | CAL-2, getSettings | my-health-medication-calendar-ui |
| 8 | CAL-7 | Empty and loading states for calendar | CAL-1 | my-health-medication-calendar-ui |

---

## Previously completed specs (pruned)

All tasks done for: **Add Vitals & Lab Results — API**, **Add Vitals & Lab Results — UI**, **Daily Recommendations — API**, **Daily Recommendations — UI**, **My Health — UI** (except calendar), **Medication Reminders**, **Prescription Medication Dialog**, **Notifications** (except NOTIF-VITALS-CATCHUP).

---

## Spec: Medication Reminders (`specs/medication-reminders.md`)

### Schema status

- [x] **MED-SCHEMA-1** `UserMedication` schema has all required fields: `name`, `dosage`, `frequency`, `timesPerDay`, `reminderTimes`, `startDate`, `endDate`, `isActive`, `sourceDocumentId` (plus `activeSubstance`, `purpose`)
  - Verified in `backend/src/patients/schemas/user-medication.schema.ts`; collection `usermedications`; indexed on `userId`. Model injected in `PatientsService`; `getMedications` uses it.

### Backend CRUD

- [x] **MED-API-1** `GET /patients/medications` — return authenticated user's medications (sorted by name)
- [x] **MED-API-2** `POST /patients/medications` — create a single medication for the authenticated user
- [x] **MED-API-3** `PATCH /patients/medications/:id` — update a medication (including reminder times, active status)
- [x] **MED-API-4** `DELETE /patients/medications/:id` — hard-delete a medication

- [x] **DOC-API-1** `GET /patients/documents/:id` — return single document for authenticated user (incl. `extractedData`)
  - Required for MED-UI-7. Add `getDocument(userId, documentId)` in `PatientsService` (findOne by _id + userId); add `@Get('documents/:id')` in `PatientsController`; return 404 if not found. Response shape aligned with list item (id, documentType, originalFilename, attachmentId, extractedData, etc.).

### Frontend — Dashboard medications table

- [x] **MED-UI-1** Add `getMedications()` and create/update/delete/batch to `frontend/src/services/patients.service.ts`
- [x] **MED-UI-2** Add "Current Medications" section to `Dashboard.tsx` — table with name, dosage, frequency, status
- [x] **MED-UI-3** Medication row tap opens edit view for reminder settings (inline or modal)

- [x] **MED-UI-7** Medication edit dialog: "Original prescription text" section when `sourceDocumentId` is set
  - Spec AC: "When the user opens the medication edit dialog from the dashboard, the dialog includes, at the end of its content, a **read-only 'Original prescription text' section** (when available) that shows the raw medication line(s) as they appeared in the source prescription." Uses medication's `sourceDocumentId` to look up `UserDocument.extractedData`.
  - Depends on DOC-API-1. Frontend: add `getDocument(id)` to patients.service; in `MedicationEditModal`, when medication has `sourceDocumentId`, fetch document and display read-only section (e.g. medication line from extractedData.medications or display full medications list as original text).

### Frontend — Local notification scheduling

- [x] **MED-UI-4** Schedule local notifications for each active medication based on `reminderTimes` (when medication reminders are enabled in settings)
- [x] **MED-UI-5** When user updates `reminderTimes`, cancel existing notifications and reschedule
- [x] **MED-UI-6** When user disables medication reminders in settings, cancel all medication notifications

---

## Spec: Prescription Medication Dialog (`specs/prescription-medication-dialog.md`)

- [x] **RX-DIALOG-API-1** `POST /patients/medications/batch` — create/update multiple medications, linked to source document
- [x] **RX-DIALOG-UI-1** Create `MedicationReviewDialog` — modal with extracted medications as editable cards
- [x] **RX-DIALOG-UI-2** Frequency-to-schedule inference logic (frontend utility)
- [x] **RX-DIALOG-UI-3** Medication card fields: name, dosage, frequency, timesPerDay, reminderTimes, startDate, endDate — all editable
- [x] **RX-DIALOG-UI-4** "+" button to manually add a medication card with defaults
- [x] **RX-DIALOG-UI-5** Scrollable medication list; dialog does not exceed viewport height
- [x] **RX-DIALOG-UI-6** Save action — call batch endpoint, show success, close dialog
- [x] **RX-DIALOG-UI-7** Cancel action — close dialog without saving; uploaded document remains
- [x] **RX-DIALOG-UI-8** Wire `AddPrescriptionForm` to open `MedicationReviewDialog` on successful upload

---

## Spec: Notifications (`specs/notifications.md`)

### Phase 1: Settings API + local notifications

- [x] **NOTIF-API-3** `GET /patients/settings` — return notification preferences
- [x] **NOTIF-API-4** `PATCH /patients/settings` — update notification preferences
- [x] **NOTIF-FE-1** Install `@capacitor/push-notifications` and `@capacitor/local-notifications`; run `npx cap sync`
- [x] **NOTIF-UI-1** Notification settings section on Profile/Settings with toggles for each type
- [x] **NOTIF-UI-2** Toggle changes call PATCH and reschedule local notifications
- [x] **NOTIF-FE-8** Schedule daily local notification for "daily recommendations" (8:00 AM default)
- [x] **NOTIF-FE-9** Schedule daily local notification for "add vitals reminder" (9:00 AM default)

### Phase 2: Device registration + push notifications (Capacitor native)

- [x] **NOTIF-API-1** `POST /notifications/register` — store device token or web push subscription
- [x] **NOTIF-API-2** `DELETE /notifications/register` — remove device token/subscription
- [x] **NOTIF-FE-2** On app launch (authenticated + native), request permission and register device token with backend
- [x] **NOTIF-FE-3** Handle denied permission gracefully — show info in settings
- [x] **NOTIF-FE-4** On logout, unregister device token from backend
- [x] **NOTIF-FE-5** Handle incoming push notifications — display and deep-link on tap
- [x] **NOTIF-UI-3** When permission denied at OS level, disable toggles with explanation text

### Phase 3: PWA web push

- [x] **NOTIF-FE-6** Push event handler in `sw.js`; `notificationclick` for deep-linking
- [x] **NOTIF-FE-7** On opt-in (web), subscribe via PushManager + VAPID and send subscription to backend

### Recommendations page bubble

- [x] **NOTIF-UI-4** Show dismissable info bubble on `/recommendations` when daily recommendations notifications not enabled

- [ ] **NOTIF-VITALS-CATCHUP** Vitals reminder catch-up bubble on web (PWA)
  - Spec AC: "If the user did **not receive or act on** the scheduled vitals reminder, then **on the next authenticated web session** (PWA) a **non-modal bubble** appears reminding them to log vitals. Bubble copy: 'You haven't logged vitals today. Tap here to add them.' with link/action to vitals form. Once shown for a given day and dismissed or acted upon, the bubble does **not** reappear again that day."
  - On relevant pages (e.g. `/add` or dashboard) when on web/PWA: check vitals reminder enabled, user has not logged vitals today, bubble not dismissed for today; show dismissable bubble with link to add vitals. Persist "shown today" in sessionStorage keyed by date.

---

## Spec: My Health — UI (`specs/my-health-ui.md`) — remaining

- [x] **MY-HEALTH-MED-TABLE** Prescription tab: medications table below documents table
  - Spec AC: "A **medications table** below the documents table: rows for **all medications** (active and inactive) from `GET /patients/medications`, ordered by **start date descending** (fallback to creation when start date missing). Columns: medication name, dosage, frequency, status (active/inactive), start date, end date, origin label ('From prescription' when `sourceDocumentId` set, else 'Manual')."
  - Done: My Health fetches medications with initial data; Prescription tab shows documents section then medications table (sorted by start date desc, origin label).

---

## Spec: My Health – Medication calendar UI (`specs/my-health-medication-calendar-ui.md`)

- [ ] **CAL-1** Reusable `MedicationCalendar` component: month grid, dots per day for reminder times, today highlight
  - Spec: Month-style grid; one cell per day; dots inside cells for scheduled medication reminder times; navigate previous/next month; highlight "today". Component accepts events (e.g. `{ date, time, medications[] }`) via props so it is reusable.

- [ ] **CAL-2** Place calendar on My Health below UserPanel, above Vitals/Prescription/Exams tabs
  - Spec AC: "When the user opens `/my-health`, **above the tabs** and below the profile/UserPanel, they see a 'Medication schedule' calendar block."

- [ ] **CAL-3** Data mapping: active medications with startDate/endDate and reminderTimes → dots per day
  - For each active medication in window startDate–endDate, for each reminderTime, the corresponding day cell shows at least one dot. Multiple meds at same time = one dot; click shows all in details.

- [ ] **CAL-4** Dot interaction: details view (date/time + list of medications at that slot)
  - Spec AC: Clicking a dot opens a details view (popover/panel/modal) with date/time and list of medications (name, dosage, frequency; optional start/end). Close via backdrop or Escape.

- [ ] **CAL-5** Month navigation and "Today" control
  - Previous/next month; "Today" jumps to current month and highlights today. Dots recalculated for visible month.

- [ ] **CAL-6** When medication reminders disabled in settings: show inline note, still show schedule
  - Spec AC: "When `notificationSettings.medicationReminder` is **false**, the calendar still displays schedule dots. An inline note appears: e.g. 'Medication notifications are off. Turn them on in Settings to receive reminders.'"

- [ ] **CAL-7** Empty and loading states
  - Loading → skeleton/indicator. No active medications with reminderTimes → empty state message (e.g. "No medications scheduled yet. Add medications or configure reminders to see them here.").

---

## Full priority table (reference)

| # | Task ID | Summary | Spec |
|---|---------|---------|------|
| 1 | NOTIF-VITALS-CATCHUP | Vitals catch-up bubble on web | notifications |
| 2 | CAL-1 | Reusable MedicationCalendar component | my-health-medication-calendar-ui |
| 3 | CAL-2 | Place calendar on My Health below UserPanel | my-health-medication-calendar-ui |
| 4 | CAL-3 | Map medications + reminderTimes to calendar dots | my-health-medication-calendar-ui |
| 5 | CAL-4 | Dot click → details view | my-health-medication-calendar-ui |
| 6 | CAL-5 | Month navigation + "Today" control | my-health-medication-calendar-ui |
| 7 | CAL-6 | Inline note when medication reminders disabled | my-health-medication-calendar-ui |
| 8 | CAL-7 | Empty and loading states for calendar | my-health-medication-calendar-ui |
